<?php require __dir__."/../layout/app.phtml"?>

<div class="container">
<div class="col-md-5">
    <div class="panel panel-default">
    <div class="panel-heading">
        <strong>Seznam knih (<?=count($books)?>)</strong>
        <div class="pull-right">
            <a href="<?=$router->pathFor('lists-edit');?>"><span class="glyphicon glyphicon-plus"></span> Přidat knihu</a>
            |
            <a href="#" id="toggle-list" data-toggle="collapse" data-target="#collapse-list">Rozbalit</a>
        </div>
    </div>
    <div class="panel-body collapse" id="collapse-list">
    <?php if (is_array($books) && count($books)):?>
        <ul class='list-group'>
        <?php foreach($books as $book):?>
        <li class='list-group-item'>
            <span style="width: 75%;display: inline-block;"><?=$book['author'] == ""?"":$book['author'] . ": "?><b><?=$book['name']?></b></span>
            <a href="#" class='btn btn-xs btn-danger pull-right'>Odstranit</a>
        </li>
        <?php endforeach;?>
        </ul>
    <?php else:?>
        <span class='text-muted'>Žádné knihy nenalezeny</span>
    <?php endif?>
    </div>
    </div>
</div>
<script>
$('#toggle-list').click(function(){
    $(this).text(function(i,old){
        return old == 'Rozbalit' ?  'Sbalit' : 'Rozbalit';
    });
});
</script>

<div class="col-md-5 col-md-offset-2">
    <div class="panel panel-default">
    <div class="panel-heading">
        <strong>Nahrát nový kánon (Stávající bude přepsán)</strong>
    </div>
    <div class="panel-body">
    <p>
    očekávaný formát csv souboru je:<br />
    <pre>[id];[region id];[author];[name];[genere id]</pre>
    </p>
    <form method="post" enctype="multipart/form-data" action="<?=$router->pathFor('admin-upload')?>">
        <input type="hidden" name="<?= $csrf['nameKey'] ?>" value="<?= $csrf['name'] ?>">
        <input type="hidden" name="<?= $csrf['valueKey'] ?>" value="<?= $csrf['value'] ?>">
        <input type="file" name="book"/><br />
        <input type="submit"/>
    </form>
    </div>
    </div>
</div>

